import React from 'react';
import { AppFooter } from '../styles'

const Footer = () => {
    return (
        <AppFooter>
            <p>Thanks for visiting 👋</p>
        </AppFooter>
    );
};

export default Footer;